---開発環境---------------
XAMPP
PHP 7.3.11
------------------------>>


---Question-1------------
タスク
1.1から100までの数字を表示
    　→繰り返しで処理

2.FizzBuzzの表示
    　→ifとelseで処理
    　順番
    　・3と5の最小公倍数である15の倍数の時にFizzBuzzと表示
    　・3の倍数の時にFizzと表示
    　・5の倍数の時にBuzzと表示
    　・条件に当てはまらない場合は数字を表示
    　

------------------------>>


---Question-2-------------
※素数（そすう、英: prime number）とは、1 より大きい自然数で、正の約数が 1 と自分自身のみであるもののことである。

・表示する表示は、割る数と割られる数が同じかつ、約数が前者意外にないもの。
・約数が1と割る数自身意外にある場合を除けば可能。

タスク（1000の素数は168ケ）
    1.繰り返し文で、2からスタートし1000より小さい値まで1ずつ足された条件下を作成
    2.１から順番に約数があるかないかを確かめ、あればbreak
    3.２に当てはまらない場合は数を表示


        例（while）：
        //初期値を0に設定
        $i = 0;
        //条件式：$iが10になるまでループして出力(echo)処理を行う
        while($i <= 10){
        echo $i;
        $i++;
        }

        例（break）：
        //$iの値が5の場合に処理(ループ処理を終了)
        if($i == 5){
            break;
        }


------------------------>>


---Question-3-------------
※多次元配列＝配列の中に配列を格納する構造（ネスト）にすること

・箱の中の箱の中身を、インデッックスまたはキー指定で取れるようにする。
・箱の最上位から最下層のデータを引っ張ること（箱の中の、箱の中身を出力）

タスク
    1.箱の中の箱を作成
            箱の中の中身を作成
    2.１をいれる箱の作成
            １を格納
    ※箱を増やす場合はこれの繰り返し（今回の方法に限るが、別にも箱を増やす方法あり）


------------------------>>

　